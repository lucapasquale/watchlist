version: '3'

services:
  api:
    image: node-image
    container_name: video-list-api
    pull_policy: build
    build:
      context: ../../
      args:
        PROJECT: api
    ports:
      - 3000:3000
    env_file:
      - ../../stack.env

