query AddItemUrlInformation($input: UrlInformationInput!) {
  urlInformation(input: $input) {
		kind
		title
		thumbnailUrl
    url
    durationSeconds
	}
}

mutation CreatePlaylistItem($input: CreatePlaylistItemInput!) {
  createPlaylistItem(input: $input) {
    id
  }
}
