query HomeHeroPlaylists {
  playlists {
    id
    name
    itemsKind
    itemsCount

    firstItem {
      id
    }

    user {
      id
      name
      initials
      profilePictureUrl
    }
  }
}
