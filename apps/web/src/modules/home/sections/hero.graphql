query HomeHeroPlaylists {
  featuredPlaylists {
    id
    name
    itemsKind
    itemsCount

    user {
      id
      name
      initials
      profilePictureUrl
    }
    firstItem {
      id
    }
  }
}
